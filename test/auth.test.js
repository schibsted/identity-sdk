'use strict';

const { expect } = require('chai');

describe('auth', () => {
    it('dummy test', () => {
        // eslint-disable-next-line no-unused-expressions
        expect(true).to.be.true;
    });
});
